# biz-ideas-hub 创始会员创建完成

## 操作内容
为用户创建创始会员账号，用户名：郝仕麟，密码：956244978

## 实现方式
修改了 `js/auth.js`，在系统初始化时自动创建创始会员：

```javascript
// 确保创始会员存在
ensureFounderExists() {
    const founderUsername = '郝仕麟';
    const founderPassword = '956244978';
    if (!this.users[founderUsername]) {
        this.users[founderUsername] = {
            username: founderUsername,
            password: this.hashPassword(founderPassword),
            role: 'member',
            createdAt: new Date().toISOString(),
            favorites: []
        };
        this.saveUsers();
        console.log('创始会员已创建:', founderUsername);
    }
}
```

## 使用说明

### 方法1：自动创建（推荐）
刷新网页，系统会自动创建创始会员账号。

### 方法2：手动创建
如果自动创建未生效，按 F12 打开浏览器控制台，粘贴运行以下代码：

```javascript
const auth = new AuthSystem();
auth.users['郝仕麟'] = {
    username: '郝仕麟',
    password: auth.hashPassword('956244978'),
    role: 'member',
    createdAt: new Date().toISOString(),
    favorites: []
};
auth.saveUsers();
console.log('创始会员创建成功！');
```

### 登录
创建成功后，使用以下信息登录：
- **用户名**: 郝仕麟
- **密码**: 956244978

### 邀请好友
登录后点击"邀请好友"按钮，即可生成30分钟内有效的邀请码。
